numpy>=1.24
torch>=2.0
matplotlib>=3.6

# Optional: only needed for exp9 (real LoRA preference fine-tuning).
# The toy (1-3,7,8) and Ollama (4-6) experiments do NOT require these.
# transformers>=4.44
# peft>=0.12
# trl>=1.6
# datasets>=2.20
# accelerate>=0.34
